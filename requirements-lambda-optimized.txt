# Dependências otimizadas para Lambda - Projeto Vida
# Foco: Baixo custo, alta performance, segurança

# Core FastAPI (versões estáveis)
fastapi==0.104.1
mangum==0.17.0

# Database (otimizado para Lambda)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Validation (versões compatíveis com Lambda)
pydantic==2.5.0
pydantic-core==2.14.1

# Security (essencial)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# AWS (nativo)
boto3==1.34.0

# Environment
python-dotenv==1.0.0

# HTTP (mínimo necessário)
requests==2.31.0

# Data Processing (otimizado para dashboard)
pandas==2.2.0
openpyxl==3.1.2

# Logging otimizado
structlog==23.2.0
