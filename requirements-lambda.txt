# Core FastAPI (otimizado para Lambda)
fastapi==0.104.0
mangum==0.17.0
python-multipart==0.0.20

# Database (apenas o necessário)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Validation (essencial)
pydantic==2.5.0
email-validator==2.1.0

# Security (otimizado)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7
slowapi==0.1.9

# AWS (essencial)
boto3==1.34.0

# Environment
python-dotenv==1.0.0

# HTTP (essencial)
requests==2.31.0

# Excel Export (otimizado)
pandas==2.1.4
openpyxl==3.1.2
